# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/tv-tracker
MONGODB_DATA_DIR=E:/MongoDB/tv-tracker-data/db
MONGODB_LOG_DIR=E:/MongoDB/tv-tracker-data/logs

# Server Configuration
PORT=3000

# Optional: MongoDB Authentication (uncomment if needed)
# MONGODB_USERNAME=your_username
# MONGODB_PASSWORD=your_password 

# --- Email Configuration ---
# Replace with your actual email provider details (e.g., Mailtrap for testing, SendGrid/Gmail for production)
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USER=26b60d9a502df6
EMAIL_PASS=f0b7d65f2a3c91

# --- JWT Configuration ---
# !! IMPORTANT: Replace this with a strong, random, and secret string !!
# You can generate one using an online generator or a command like: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=26627ab92fd4eecf14f2934506265499b86cb4133f0cd78bac2d734cae683bef

# Optional: Set token expiration time (e.g., 90d, 1h, 7d)
JWT_EXPIRES_IN=90d

# --- Database Configuration ---
# (You likely already have this, ensure it's correct)
# MONGODB_URI=mongodb://localhost:27017/tv-tracker

# --- Other ---
# NODE_ENV=development # Or production